# Every markdown file in the collection is UTF-8 with LF line endings, so that the
# ^...$ anchored patterns books.json declares match whether a consumer reads the
# file from a checkout or over HTTP. Do not let git rewrite them on checkout.
*.md text eol=lf
*.json text eol=lf
*.pdf binary
*.png binary
*.mp3 binary
*.html -text
